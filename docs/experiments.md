# Experiments

Generated by `cargo run --example planner_experiments -- --write-docs`.

This repository treats design work as a sequence of comparable experiments. Stable runtime code stays in `crates/`; design candidates stay in `experiments/` until repeated case sets justify promotion.

## Active Suites

- `cross_provider_validation`: validate the current planner frontier across provider adapters.
- `cross_suite_contradiction`: detect when local suite frontiers and promotion policy disagree.
- `cross_train_lag_carryover`: decide whether unresolved publication lag should continue, resolve, or block across release cuts.
- `frontier_snapshot_replay`: replay versioned provider/model evidence before promoting a provisional frontier.
- `planner_selection`: choose which YAML skill should run.
- `artifact_trust_decay`: decide how artifact trust should fall off when changelog, release notes, and rollback notes disagree across release trains.
- `artifact_mirroring_drift`: decide how publication mirrors should drift when package registries and docs portals fall out of sync.
- `provenance_lag_budgets`: decide when staggered publication delays should stay pending, become superseded, or block provenance updates.
- `cross_repo_provenance_stitching`: decide how provenance should be stitched when release evidence is split across multiple repositories.
- `provenance_backfill`: decide whether missing release provenance can be safely reconstructed from changelog and release-note artifacts.
- `promotion_environment_provenance`: decide whether a promoted runtime surface still has coherent provenance across deployment environments.
- `promotion_provenance`: decide whether a promoted runtime surface still has a continuous documented release lineage.
- `promotion_rules`: decide when an experimental frontier is mature enough to move into the stable runtime.
- `planner_prompt_shaping`: choose how planner prompt text and schema constraints should be presented.
- `recovery_skill_patterns`: choose what kind of recovery skill sequence should run after a failure.
- `rollback_rules`: decide when a previously promoted runtime surface should stay, roll back, or be replaced.
- `ros2_observation_ingestion`: choose how ROS2 topic streams should reduce into replanning context.
- `surface_lag_budget_calibration`: choose how each publication surface should calibrate its lag budget from observed traces.
- `resume_policy`: choose which step to resume from after recovery.
- `gateway_replanning`: choose what the gateway loop does after each execution outcome.
- `backend_observation_fusion`: choose how backend state and sensor observations are fused before replanning.
- `tool_output_validation`: choose how `expect` contracts should match tool outputs before retry or replan.

## Cross-Provider Validation

Planner frontiers should be validated across real provider adapters instead of assuming provider-neutral behavior from mock-only comparisons.

| case | expected skill | why |
| --- | --- | --- |
| `pick_basic` | `pick_and_place` | Baseline transport task for every provider. |
| `wave_basic` | `wave_arm` | Simple non-recovery gesture instruction. |
| `recover_grasp` | `recover_grasp` | Canonical grasp recovery turn. |
| `recover_observation` | `recover_observation` | Canonical observation recovery turn. |
| `recover_compact_goal` | `recover_grasp` | Compact user goals stress provider reliance on recovery context instead of task wording. |
| `wave_motion_interrupt` | `wave_arm` | If no recovery skill matches, providers should still be free to return the task skill. |

| provider | availability | attempted cases | accuracy | avg latency ms | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `mock` | `available` | 6 | 83.3% | 0.03 | 100 | 100 |
| `local` | `unavailable` | 0 | 0.0% | n/a | 100 | 100 |
| `openai` | `disabled` | 0 | 0.0% | n/a | 100 | 100 |
| `claude` | `disabled` | 0 | 0.0% | n/a | 100 | 100 |


## Cross-Suite Contradiction Detection

Local suite frontiers and stable-runtime promotion policy should be compared explicitly so contradictory signals become new experiment work instead of hidden architecture debt.

| case | suite | expected detection | conflicting reference | why |
| --- | --- | --- | --- | --- |
| `declared_checkpoint_aligned` | `resume_policy` | `no_contradiction` | `none` | Local frontier, replay, and promotion all point at the same narrow runtime move. |
| `planner_split_hold` | `planner_selection` | `no_contradiction` | `none` | A split frontier with a hold decision is an explicit unresolved state, not a contradiction. |
| `pipeline_router_switch_within_frontier` | `planner_selection` | `no_contradiction` | `none` | Promotion can switch to another member of the local frontier without contradicting the suite. |
| `prompt_reference_escape` | `planner_prompt_shaping` | `reference_conflict` | `schema_constrained` | Promotion points at a candidate that is not in the local frontier at all. |
| `gateway_premature_promotion` | `gateway_replanning` | `premature_promotion` | `resume_aware_replan` | Promotion moved ahead of replay evidence even though the suite still needs more pressure. |
| `tool_risk_ignored` | `tool_output_validation` | `risk_conflict` | `tolerant_contract` | Promotion ignored the fact that the broader matcher still carries too much runtime ambiguity. |
| `ros2_hold_consistent` | `ros2_observation_ingestion` | `no_contradiction` | `none` | Holding an environment-specific frontier outside core is internally consistent. |
| `provider_mock_promotion_conflict` | `cross_provider_validation` | `risk_conflict` | `mock` | Provider validation is environment-bound, so stable promotion here is contradictory. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `frontier_membership_only` | membership check | 62.5% | 0.15 | 2.00 | 87 | 88 |
| `promotion_kind_only` | decision kind only | 75.0% | 0.14 | 2.00 | 83 | 100 |
| `risk_signal_gate` | risk signal gate | 87.5% | 0.18 | 4.00 | 63 | 86 |
| `evidence_graph` | evidence graph | 100.0% | 0.18 | 6.00 | 51 | 70 |


## Cross-Train Lag Carryover

Unresolved publication lag should carry across release cuts only through explicit budgets, instead of treating every new train as either a full reset or a full continuation.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `single_pending_cut_stays_pending` | `planner_selection` | `carryover_pending` | `none` | A single pending cut should not spend cross-train lag debt yet. |
| `same_challenger_two_cuts_supersedes` | `planner_selection` | `carryover_superseded` | `pipeline_router` | The same challenger surviving across consecutive pending cuts should spend the carryover budget. |
| `mixed_challenger_chain_blocks` | `planner_prompt_shaping` | `carryover_blocked` | `none` | Carryover debt should block when consecutive pending cuts point to different challengers. |
| `confirmed_train_clears_old_pending` | `gateway_replanning` | `carryover_confirmed` | `resume_aware_replan` | A later confirmed train should clear carryover debt from older pending cuts. |
| `latest_rollback_blocks_chain` | `tool_output_validation` | `carryover_blocked` | `none` | A rollback in the latest cut should block regardless of older pending history. |
| `intervening_confirmed_resets_carryover` | `promotion_provenance` | `carryover_pending` | `none` | A confirmed cut in the middle should reset carryover debt instead of chaining old pending lag forever. |
| `docs_only_repeated_pending_stays_pending` | `promotion_environment_provenance` | `carryover_pending` | `none` | Repeated docs-only drift should still stay pending until critical surfaces join or the lag debt truly expires. |
| `latest_decisive_supersede_without_carryover` | `cross_provider_validation` | `carryover_superseded` | `local` | A latest train with a decisive critical-surface challenger should supersede even without older carryover debt. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `latest_train_only` | latest train | 75.0% | 1.30 | 1.00 | 79 | 70 |
| `adjacent_pending_reuse` | 1-step carry | 87.5% | 3.02 | 3.00 | 51 | 36 |
| `surface_chain_vote` | chain vote | 25.0% | 1.59 | 4.00 | 42 | 38 |
| `carryover_budget` | carryover budget | 100.0% | 3.88 | 6.00 | 0 | 10 |


## Frontier Snapshot Replay

Frontier promotion should replay versioned provider/model snapshots instead of depending on a single current validation run.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `prompt_constrained_stable` | `planner_prompt_shaping` | `promote_reference` | `prompt_constrained` | The provisional reference stays ahead across multiple providers and recent snapshots. |
| `planner_split_hold` | `planner_selection` | `hold_experimental` | `none` | The frontier is split across providers and ties are common, so replay should not promote. |
| `pipeline_router_takeover` | `planner_selection` | `switch_reference` | `pipeline_router` | Newer provider snapshots consistently favor the rival candidate. |
| `mock_only_not_enough` | `cross_provider_validation` | `hold_experimental` | `none` | A single mock snapshot is not enough evidence to promote a frontier for provider validation. |
| `frontier_recovered_after_regression` | `planner_prompt_shaping` | `promote_reference` | `prompt_constrained` | Recent multi-provider evidence outweighs an older regression. |
| `noise_tie_hold` | `tool_output_validation` | `hold_experimental` | `none` | When replay evidence is mostly ties and tiny margins, promotion should wait for more snapshots. |

| variant | style | accuracy | avg us | avg evidence count | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `latest_snapshot_only` | latest snapshot | 83.3% | 0.33 | 3.33 | 91 | 86 |
| `provider_majority` | provider majority | 83.3% | 0.53 | 3.33 | 67 | 74 |
| `strict_consensus_gate` | strict consensus | 66.7% | 0.51 | 3.33 | 71 | 82 |
| `weighted_stability` | weighted replay | 100.0% | 0.82 | 3.33 | 52 | 100 |


## Planner Selection

Planner skill selection should evolve through comparable experiments instead of one-off abstraction.

The stable runtime stays in `crates/`; planner variants live in `experiments/planner_selection/` and are judged on the same instruction set.

| case | expected | why |
| --- | --- | --- |
| `pick-basic` | `pick_and_place` | Baseline pick and place flow. |
| `pick-compact` | `pick_and_place` | Short phrasing should still select the transport skill. |
| `wave-acknowledge` | `wave_arm` | Canonical greeting request. |
| `wave-salute` | `wave_arm` | Tests phrasing beyond the obvious wave keyword. |
| `wave-gesture` | `wave_arm` | Tests whether variants generalize gesture language. |
| `transport-phrasing` | `pick_and_place` | Tests transport phrasing without explicit pick/place keywords. |
| `recover-grasp` | `recover_grasp` | Canonical grasp recovery context. |
| `recover-observation` | `recover_observation` | Canonical sensor recovery context. |

| variant | style | accuracy | avg us | readability | extensibility | source |
| --- | --- | --- | --- | --- | --- | --- |
| `current_heuristic_clone` | current baseline | 87.5% | 17.87 | 40 | 82 | `experiments/planner_selection/current_heuristic_clone.rs` |
| `keyword_rules` | imperative rules | 75.0% | 3.67 | 51 | 46 | `experiments/planner_selection/keyword_rules.rs` |
| `catalog_score` | scored ranking | 100.0% | 49.92 | 59 | 100 | `experiments/planner_selection/catalog_score.rs` |
| `pipeline_router` | pipeline router | 100.0% | 4.53 | 31 | 88 | `experiments/planner_selection/pipeline_router.rs` |


## Artifact Trust Decay

Artifact trust should decay across release trains when changelog, release notes, and rollback notes disagree, instead of treating every artifact mention as equally stable forever.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `stable_current_across_trains` | `resume_policy` | `trust_confirmed` | `declared_checkpoint` | Repeated current-reference agreement across trains should preserve trust. |
| `consistent_challenger_supersedes` | `planner_selection` | `trust_superseded` | `pipeline_router` | Repeated challenger support across later trains should shift trust to the replacement reference. |
| `latest_train_conflict_decay` | `planner_prompt_shaping` | `trust_decay` | `none` | A conflicting latest train should reduce trust instead of selecting a winner immediately. |
| `rollback_note_rejected` | `tool_output_validation` | `trust_rejected` | `none` | A strong rollback note in the latest train should invalidate artifact trust. |
| `stale_rollback_resolved_confirmed` | `gateway_replanning` | `trust_confirmed` | `resume_aware_replan` | An old rollback should not dominate if later trains repeatedly re-establish the same reference. |
| `latest_sparse_note_decay` | `backend_observation_fusion` | `trust_decay` | `none` | A sparse low-confidence latest note should not carry trust by itself. |
| `history_outvotes_latest_noise_confirmed` | `ros2_observation_ingestion` | `trust_confirmed` | `stream_reducer` | A single noisy latest changelog should not outweigh stable historical agreement. |
| `stale_rollback_unresolved_decay` | `cross_provider_validation` | `trust_decay` | `none` | Later weak reassurance is not enough to erase an older rollback signal. |
| `latest_release_notes_supersede` | `promotion_provenance` | `trust_superseded` | `lineage_budget` | A strong latest release train can legitimately move trust to a challenger when both primary artifacts agree. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `latest_train_only` | latest train only | 55.6% | 0.19 | 1.00 | 70 | 56 |
| `static_source_priority` | static source priority | 55.6% | 0.27 | 3.00 | 76 | 76 |
| `per_train_majority` | per-train majority | 55.6% | 0.84 | 4.00 | 43 | 38 |
| `trust_decay_budget` | trust decay budget | 100.0% | 8.62 | 6.00 | 0 | 36 |


## Artifact Mirroring Drift

Mirrored release artifacts should be treated as drifting signals across package registries and documentation portals, not as one perfectly synchronized publication surface.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `aligned_current_all_mirrors` | `resume_policy` | `mirror_confirmed` | `declared_checkpoint` | Fresh agreement across all mirrors should preserve the current reference. |
| `fresh_pair_challenger_supersedes` | `planner_selection` | `mirror_superseded` | `pipeline_router` | Fresh agreement from the strongest mirrors should move the train to the challenger. |
| `stale_registry_noise_current` | `ros2_observation_ingestion` | `mirror_confirmed` | `stream_reducer` | A stale registry mirror should not outweigh fresh docs and api mirrors. |
| `fresh_conflict_same_train_drift` | `planner_prompt_shaping` | `mirror_drift` | `none` | Fresh disagreement between registry and docs should stay unresolved. |
| `package_yank_rejected` | `tool_output_validation` | `mirror_rejected` | `none` | A fresh registry yank should veto the mirrored publication train. |
| `stale_yank_resolved_confirmed` | `gateway_replanning` | `mirror_confirmed` | `resume_aware_replan` | An older yank should not dominate after a later train re-establishes all mirrors. |
| `single_portal_challenger_drift` | `promotion_provenance` | `mirror_drift` | `none` | A single portal mirror is not enough to declare a new authoritative train. |
| `release_feed_pair_supersedes` | `cross_provider_validation` | `mirror_superseded` | `local` | A fresh feed plus docs pair can supersede a stale package mirror. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `registry_only` | registry only | 62.5% | 0.22 | 1.00 | 78 | 66 |
| `docs_portal_preferred` | docs portal preferred | 62.5% | 0.24 | 3.00 | 67 | 72 |
| `latest_mirror_majority` | latest majority | 75.0% | 1.08 | 4.00 | 44 | 34 |
| `mirror_budget` | mirror budget | 100.0% | 8.60 | 6.00 | 0 | 42 |


## Provenance Lag Budgets

Release provenance should tolerate bounded publication lag across registries, release feeds, and docs portals instead of treating every unsynchronized surface as immediate contradiction.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `aligned_current_all_surfaces` | `resume_policy` | `lag_confirmed` | `declared_checkpoint` | When every publication surface agrees within its lag budget, the current reference should stay confirmed. |
| `feed_and_docs_ahead_registry_within_budget_pending` | `planner_selection` | `lag_pending` | `none` | Feed and docs can move ahead of the registry while the registry is still within its publication lag budget. |
| `registry_overdue_after_budget_supersede` | `planner_selection` | `lag_superseded` | `pipeline_router` | Once the registry delay exceeds its budget, the challenger pair from feed and docs should supersede the old current reference. |
| `single_docs_surface_ahead_pending` | `promotion_provenance` | `lag_pending` | `none` | A single early docs update is not enough to spend the lag budget. |
| `registry_yank_blocks_latest_train` | `tool_output_validation` | `lag_blocked` | `none` | A fresh package-registry yank should block promotion even if slower surfaces still show the old state. |
| `fresh_current_after_previous_rollback_confirmed` | `gateway_replanning` | `lag_confirmed` | `resume_aware_replan` | A later train that re-establishes all surfaces within budget should clear the earlier rollback train. |
| `persistent_split_past_budget_blocked` | `planner_prompt_shaping` | `lag_blocked` | `none` | If current signals stay overdue but challenger evidence never spreads beyond one surface, the lag budget is exhausted and the train stays blocked. |
| `registry_and_feed_challenger_docs_can_lag` | `cross_provider_validation` | `lag_superseded` | `local` | Registry and release-feed agreement can supersede the docs portal while docs is still inside its lag budget. |
| `all_surfaces_challenger_supersede` | `planner_selection` | `lag_superseded` | `pipeline_router` | Once every surface converges on the challenger within budget, the old reference should be superseded. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `latest_registry_only` | registry only | 55.6% | 0.22 | 1.00 | 76 | 64 |
| `freshest_signal_wins` | freshest signal | 55.6% | 0.21 | 3.00 | 76 | 64 |
| `surface_majority` | surface majority | 66.7% | 1.14 | 4.00 | 44 | 34 |
| `lag_budget` | lag budget | 100.0% | 2.43 | 6.00 | 0 | 16 |


## Cross-Repo Provenance Stitching

Release provenance should be stitchable across repositories only through comparable join policies, instead of assuming all evidence lives in one repo.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `runtime_only_confirmed` | `resume_policy` | `stitched_confirmed` | `declared_checkpoint` | A complete runtime-repo chain should still stitch to the current reference. |
| `split_repo_replace_to_current` | `gateway_replanning` | `stitched_superseded` | `resume_aware_replan` | A replacement chain can span runtime, docs, and release repos and still point to the current promoted surface. |
| `split_repo_confirmed_continuity` | `backend_observation_fusion` | `stitched_confirmed` | `failure_aware_merge` | A continuity chain may need different repositories for different release tags. |
| `conflicting_same_tag_rejected` | `planner_prompt_shaping` | `stitched_rejected` | `none` | A strong same-tag contradiction across repos should reject the stitched chain. |
| `external_pair_supersedes_current` | `planner_selection` | `stitched_superseded` | `pipeline_router` | The latest pair of non-runtime artifacts can show that the current runtime surface has already been superseded. |
| `release_repo_rollback_rejected` | `cross_provider_validation` | `stitched_rejected` | `none` | A rollback note in a different repo should still veto the stitched provenance. |
| `all_repo_agree_current` | `tool_output_validation` | `stitched_confirmed` | `tolerant_contract` | When every repo agrees per tag, the stitched chain should stay simple. |
| `tag_pair_needed_for_latest_release` | `promotion_provenance` | `stitched_superseded` | `lineage_budget` | A later replacement can be stitched only when the latest tag is joined across repositories. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `runtime_repo_only` | runtime repo only | 25.0% | 1.13 | 1.00 | 90 | 100 |
| `docs_repo_preferred` | docs repo preferred | 87.5% | 1.66 | 3.00 | 83 | 100 |
| `tag_join` | exact tag join | 12.5% | 2.49 | 4.00 | 80 | 96 |
| `stitching_budget` | stitching budget | 100.0% | 3.95 | 6.00 | 1 | 40 |


## Provenance Backfill

Missing release provenance should be recoverable from changelog and release-note artifacts only through comparable backfill policies, not one-off manual edits.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `documented_chain_confirmed` | `resume_policy` | `backfill_confirmed` | `declared_checkpoint` | No release gap exists, so backfill policy should simply preserve the current provenance chain. |
| `single_gap_backfilled_from_changelog` | `resume_policy` | `backfill_confirmed` | `declared_checkpoint` | A single high-confidence changelog entry can repair a simple continuity gap. |
| `replacement_gap_backfilled_from_dual_sources` | `planner_selection` | `backfill_superseded` | `pipeline_router` | Matching changelog and release-note artifacts should recover the missing replacement step. |
| `conflicting_artifacts_rejected` | `planner_prompt_shaping` | `backfill_rejected` | `none` | Conflicting trusted artifacts should block backfill instead of silently rewriting history. |
| `low_confidence_backfill_gap` | `gateway_replanning` | `backfill_gap` | `none` | A weak single-source note is not enough to repair missing provenance. |
| `rollback_note_rejected` | `tool_output_validation` | `backfill_rejected` | `none` | A trusted rollback note should veto provenance repair. |
| `notes_only_backfill_confirmed` | `backend_observation_fusion` | `backfill_confirmed` | `failure_aware_merge` | A strong release-note entry should be sufficient when no conflicting artifact exists. |
| `ambiguous_changelog_gap` | `ros2_observation_ingestion` | `backfill_gap` | `none` | An ambiguous challenger note should stay unresolved instead of rewriting the current lineage. |
| `documented_superseded_chain` | `promotion_provenance` | `backfill_superseded` | `lineage_budget` | Backfill policy should still recognize a fully documented replacement chain. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `documented_only` | documented only | 44.4% | 0.36 | 1.00 | 87 | 100 |
| `changelog_first` | changelog first | 77.8% | 0.86 | 3.00 | 75 | 98 |
| `dual_artifact_match` | dual source match | 77.8% | 1.03 | 4.00 | 69 | 92 |
| `artifact_lineage_budget` | artifact budget | 100.0% | 1.28 | 6.00 | 44 | 88 |


## Promotion Environment Provenance

Promotion provenance should stay coherent across deployment environments, not just across release cuts, before stable-runtime trust is increased.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `declared_checkpoint_confirmed` | `resume_policy` | `environment_confirmed` | `declared_checkpoint` | Critical and non-critical environments all agree on the same promoted surface. |
| `pipeline_router_superseded` | `planner_selection` | `environment_superseded` | `pipeline_router` | Most environments have moved to the challenger even though one critical environment is still lagging. |
| `critical_consensus_prevents_supersede` | `ros2_observation_ingestion` | `environment_gap` | `none` | Non-critical environments prefer the challenger, but critical environments still converge on the current reference. |
| `prompt_gap_missing_hardware` | `planner_prompt_shaping` | `environment_gap` | `none` | A missing critical provenance note leaves the deployment story incomplete. |
| `tolerant_contract_blocked` | `tool_output_validation` | `environment_blocked` | `none` | A critical environment has already forced rollback, so deployment provenance is blocked. |
| `stream_reducer_gap_noncritical` | `ros2_observation_ingestion` | `environment_gap` | `none` | Even a non-critical undocumented environment leaves the rollout provenance incomplete. |
| `resume_aware_confirmed` | `gateway_replanning` | `environment_confirmed` | `resume_aware_replan` | The promoted loop survives across both lab and field environments. |
| `mock_provider_blocked` | `cross_provider_validation` | `environment_blocked` | `none` | Critical deployment environments reject the promoted provider behavior. |
| `failure_aware_merge_gap` | `backend_observation_fusion` | `environment_gap` | `none` | Critical environment replacement without documentation leaves the rollout lineage incomplete. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `latest_environment_only` | latest environment | 55.6% | 0.15 | 1.00 | 78 | 66 |
| `documented_majority` | documented majority | 88.9% | 0.51 | 3.00 | 71 | 78 |
| `critical_environment_gate` | critical env gate | 66.7% | 0.36 | 4.00 | 75 | 72 |
| `environment_lineage_budget` | environment budget | 100.0% | 3.27 | 6.00 | 0 | 70 |


## Promotion Provenance

Promotion history should stay inspectable across release cuts so stable-runtime trust has a documented lineage instead of a single latest-state claim.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `declared_checkpoint_continuous` | `resume_policy` | `provenance_confirmed` | `declared_checkpoint` | The same promoted surface is documented continuously across release cuts. |
| `pipeline_router_superseded_chain` | `planner_selection` | `provenance_superseded` | `pipeline_router` | The current promoted surface replaced an older one through an explicitly documented chain. |
| `prompt_history_gap` | `planner_prompt_shaping` | `provenance_gap` | `none` | One release cut omitted provenance, so the current stable story needs backfill. |
| `tolerant_contract_broken` | `tool_output_validation` | `provenance_broken` | `none` | A rollback breaks the current provenance chain even if older cuts were documented. |
| `stream_reducer_pending_gap` | `ros2_observation_ingestion` | `provenance_gap` | `none` | A deferred cut without provenance note leaves the release history incomplete. |
| `resume_aware_replan_replace_gap` | `gateway_replanning` | `provenance_gap` | `none` | The replacement happened, but the release cut that did it did not carry provenance. |
| `mock_provider_broken_after_replace` | `cross_provider_validation` | `provenance_broken` | `none` | A documented replacement chain still ends broken if the current surface is rolled back. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `latest_cut_only` | latest cut | 42.9% | 0.15 | 1.00 | 82 | 66 |
| `documented_chain` | documented chain | 100.0% | 0.18 | 3.00 | 79 | 84 |
| `release_majority` | release majority | 42.9% | 0.64 | 2.00 | 80 | 78 |
| `lineage_budget` | lineage budget | 100.0% | 0.96 | 6.00 | 40 | 98 |


## Promotion Rules

Promotion into the stable runtime should be earned through replay, adjacent-suite support, and runtime budget signals instead of being inferred from a single frontier win.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `declared_checkpoint_promote` | `resume_policy` | `promote_reference` | `declared_checkpoint` | Checkpoint metadata is narrow, repeatedly supported by adjacent suites, and low risk to expose in the stable runtime. |
| `stream_reducer_hold` | `ros2_observation_ingestion` | `hold_experimental` | `none` | The event reducer wins the suite, but it still depends on transport-specific telemetry behavior and wider runtime surface. |
| `tolerant_contract_hold` | `tool_output_validation` | `hold_experimental` | `none` | The broader matcher wins synthetic cases, but real output ambiguity is still under-sampled. |
| `failure_aware_merge_hold` | `backend_observation_fusion` | `hold_experimental` | `none` | Fusion policy is still tied to simulator and telemetry assumptions, so it should not move into core yet. |
| `prompt_constrained_hold` | `planner_prompt_shaping` | `hold_experimental` | `none` | Prompt wording is front-runner material, but it still shifts with provider behavior and belongs in the experiment region. |
| `pipeline_router_switch` | `planner_selection` | `switch_reference` | `pipeline_router` | The rival keeps the same accuracy with lower runtime surface and stronger cross-suite support. |
| `mock_provider_hold` | `cross_provider_validation` | `hold_experimental` | `none` | Provider validation is environment-specific by definition, so even a temporary winner should stay outside the stable runtime. |
| `resume_aware_replan_hold` | `gateway_replanning` | `hold_experimental` | `none` | The hybrid loop is promising, but it still needs richer recovery chains before it should define stable gateway behavior. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `accuracy_first` | raw accuracy | 12.5% | 0.15 | 1.00 | 90 | 84 |
| `replay_gate` | replay gate | 75.0% | 0.14 | 3.00 | 83 | 80 |
| `surface_guard` | surface guard | 87.5% | 0.12 | 4.00 | 66 | 92 |
| `stability_budget` | stability budget | 100.0% | 0.39 | 6.00 | 50 | 92 |


## Planner Prompt Shaping

Planner prompt shaping should evolve through comparable prompt/schema variants instead of freezing one instruction wrapper before provider behavior is understood.

| case | expected skill | recovery turn | why |
| --- | --- | --- | --- |
| `pick_basic` | `pick_and_place` | `false` | Baseline transport instruction should remain stable across all prompt shapes. |
| `wave_basic` | `wave_arm` | `false` | Simple greeting instructions should not require any recovery-specific scaffolding. |
| `recover_grasp_transport` | `recover_grasp` | `true` | Without explicit recovery constraints a planner can drift back to pick_and_place because the original task wording dominates. |
| `recover_observation_transport` | `recover_observation` | `true` | Observation recovery should be selected even though the user goal still sounds like transport. |
| `recover_grasp_compact_goal` | `recover_grasp` | `true` | Compact goals depend heavily on prompt/schema constraints because the task text itself is underspecified. |
| `wave_motion_interrupt` | `wave_arm` | `true` | If no matching recovery skill exists, constrained prompts should still permit the task skill instead of forcing a false recovery. |

| variant | style | accuracy | avg us | avg prompt chars | avg constraint signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `catalog_freeform` | catalog freeform | 50.0% | 86.07 | 1017.17 | 0.00 | 98 | 100 |
| `prompt_constrained` | prompt constrained | 100.0% | 103.99 | 1185.83 | 1.67 | 86 | 100 |
| `schema_constrained` | schema constrained | 66.7% | 105.10 | 1159.17 | 2.17 | 93 | 100 |
| `mirrored_constraints` | mirrored constraints | 66.7% | 141.41 | 2066.33 | 4.50 | 89 | 100 |


## Recovery Skill Patterns

Recovery skill design should evolve through competing patterns instead of freezing the first specialized skill set into the runtime.

| case | failed step | expected pattern | expected resume | why |
| --- | --- | --- | --- | --- |
| `detect_sensor_miss` | `detect_object` | `reset_rescan` | `detect_object` | A sensor miss should reset observation pose and rebuild perception before retrying detection. |
| `grasp_target_still_visible` | `grasp` | `pregrasp_verify` | `move_to_object` | A failed grasp with a still-visible target should back up to pre-grasp and verify before retry. |
| `grasp_conflicting_hold_state` | `grasp` | `verify_hold` | `place_object` | If the backend thinks the cube is already held, recovery should verify the hold and continue from placement. |
| `place_failure_still_holding` | `place_object` | `reconfirm_place` | `place_object` | Placement failures that still hold the cube should reconfirm the bin pose and retry placement. |
| `wave_motion_interrupt` | `move_to_wave_peak` | `direct_resume` | `move_to_wave_peak` | Pure gesture motion interruptions usually do not justify a dedicated recovery skill. |
| `sensor_budget_exhausted` | `detect_object` | `manual_handoff` | `none` | Repeated observation failure at the budget limit should stop autonomous recovery and escalate. |

| variant | style | accuracy | avg us | avg recovery steps | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `single_step_retry` | micro retry | 33.3% | 0.35 | 0.83 | 77 | 66 |
| `always_reset_rescan` | global reset | 33.3% | 0.40 | 1.83 | 94 | 100 |
| `verification_first` | verification first | 83.3% | 0.42 | 1.67 | 62 | 72 |
| `checkpoint_aware_specialist` | checkpoint aware | 100.0% | 0.40 | 1.67 | 51 | 76 |


## Rollback Rules

Previously promoted runtime surface should be eligible for rollback through comparable rules instead of sticky one-way promotion.

| case | suite | expected decision | expected reference | why |
| --- | --- | --- | --- | --- |
| `declared_checkpoint_keep` | `resume_policy` | `keep_promoted` | `declared_checkpoint` | The promoted surface still leads locally and no rollback pressure exists. |
| `pipeline_router_replace` | `planner_selection` | `replace_reference` | `pipeline_router` | The challenger has clearly taken over, so replacement is cleaner than keeping or just rolling back. |
| `prompt_drift_defer` | `planner_prompt_shaping` | `defer_rollback` | `none` | Provider drift is visible, but one weak regression is not enough to roll back prompt shaping yet. |
| `tolerant_contract_rollback` | `tool_output_validation` | `rollback_reference` | `tolerant_contract` | The promoted matcher is now too risky to keep in stable runtime even though it still wins the local suite. |
| `mock_provider_rollback` | `cross_provider_validation` | `rollback_reference` | `mock` | Environment-bound provider behavior should leave stable runtime once it proves fragile. |
| `stream_reducer_defer` | `ros2_observation_ingestion` | `defer_rollback` | `none` | Telemetry shape is unstable, but there is not enough evidence yet to force a rollback. |
| `resume_aware_replan_keep` | `gateway_replanning` | `keep_promoted` | `resume_aware_replan` | Minor noise should not roll back a stable promoted loop. |
| `failure_aware_merge_defer` | `backend_observation_fusion` | `defer_rollback` | `none` | Fusion is shaky, but rollback should wait until either incidents worsen or a replacement is credible. |
| `provider_switch_but_rollback` | `cross_provider_validation` | `rollback_reference` | `mock` | Even if the challenger looks better, provider validation remains too environment-bound to replace one promoted provider with another. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `frontier_drop_only` | frontier drop | 33.3% | 0.17 | 2.00 | 85 | 90 |
| `incident_reactive` | incident reactive | 66.7% | 0.16 | 4.00 | 83 | 92 |
| `replay_reversal_gate` | replay reversal | 88.9% | 0.15 | 4.00 | 72 | 88 |
| `rollback_budget` | rollback budget | 100.0% | 0.58 | 6.00 | 0 | 100 |


## ROS2 Observation Ingestion

ROS2 and Gazebo observations should be ingested through comparable event-reduction shapes instead of freezing one subscriber topology into the runtime.

| case | events | expected skill | expected pose | why |
| --- | --- | --- | --- | --- |
| `detect_failure_snapshot` | 4 | `pick_and_place` | `home` | A basic failed-detection snapshot should preserve state-derived pose and the failed step. |
| `grasp_recovery_started` | 4 | `recover_grasp` | `table/front_left/pre_grasp` | When recovery starts, ingestion should keep the failed step and checkpoint resume target together. |
| `resume_after_recovery` | 5 | `pick_and_place` | `table/front_left/pre_grasp` | After recovery the reducer should preserve the resume target even if a later backend event has no skill context. |
| `wave_motion_only` | 4 | `wave_arm` | `gesture/wave_peak` | Pure motion telemetry may have no state message, so ingestion should still infer pose and movement. |
| `place_failure_still_holding` | 5 | `pick_and_place` | `bin_a` | A placement failure should preserve the held-object fact together with the placement checkpoint. |
| `stale_state_then_wave` | 4 | `wave_arm` | `gesture/wave_start` | A new skill selection should clear stale failure state and allow joint telemetry to override old pose snapshots. |

| variant | style | accuracy | avg us | avg context fields | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `state_only_snapshot` | state only | 16.7% | 0.64 | 3.33 | 98 | 98 |
| `latest_topic_join` | latest topic join | 33.3% | 1.07 | 3.50 | 88 | 78 |
| `action_state_join` | action-state join | 66.7% | 1.34 | 4.00 | 50 | 82 |
| `stream_reducer` | event reducer | 100.0% | 1.85 | 4.00 | 22 | 100 |


## Surface Lag Budget Calibration

Surface-specific publication lag budgets should be calibrated from observed traces instead of hard-coding one global delay model across registries, release feeds, and docs portals.

| case | suite | surface | expected decision | expected budget hours | why |
| --- | --- | --- | --- | --- | --- |
| `feed_fast_tighten` | `cross_provider_validation` | `release_feed` | `budget_lower` | `6` | Consistently fast feed publication should tighten the budget. |
| `docs_moderate_keep` | `planner_prompt_shaping` | `docs_portal` | `budget_keep` | `12` | Moderate but stable docs lag should keep the existing budget. |
| `registry_slow_raise` | `planner_selection` | `package_registry` | `budget_raise` | `30` | A slower registry trace should raise the registry-specific lag budget. |
| `api_docs_very_fast_tighten` | `ros2_observation_ingestion` | `api_docs` | `budget_lower` | `6` | API docs can be calibrated tighter when the trace stays consistently fast. |
| `registry_spiky_hold` | `gateway_replanning` | `package_registry` | `budget_hold` | `24` | A registry trace with multiple severe outliers should hold calibration instead of rewriting the budget. |
| `docs_speedup_tighten` | `promotion_provenance` | `docs_portal` | `budget_lower` | `12` | Docs that have sped up enough should tighten back down to the docs floor. |
| `feed_recent_regression_raise` | `promotion_environment_provenance` | `release_feed` | `budget_raise` | `10` | A feed with a clear recent regression should raise its budget beyond the old default. |
| `docs_one_outlier_keep` | `tool_output_validation` | `docs_portal` | `budget_keep` | `12` | A single docs outlier should not force either a raise or a hold. |

| variant | style | accuracy | avg us | avg consumed signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `flat_defaults` | flat defaults | 62.5% | 0.13 | 1.00 | 100 | 100 |
| `latest_observation` | latest sample | 87.5% | 0.16 | 3.00 | 100 | 100 |
| `max_observed` | max observed | 75.0% | 0.25 | 4.00 | 99 | 100 |
| `trace_budget` | trace budget | 100.0% | 0.67 | 6.00 | 80 | 100 |


## Resume Policy

Resume policy after recovery should be discovered through comparable strategies, not frozen as a hidden gateway rule.

| case | skill | failed step | expected resume | why |
| --- | --- | --- | --- | --- |
| `pick-detect` | `pick_and_place` | `detect_object` | `detect_object` | Detection failures should retry detection directly. |
| `pick-grasp` | `pick_and_place` | `grasp` | `move_to_object` | Current YAML declares a pre-grasp checkpoint before retrying grasp. |
| `pick-place` | `pick_and_place` | `place_object` | `place_object` | Place failures should not restart the whole skill. |
| `wave-peak` | `wave_arm` | `move_to_wave_peak` | `move_to_wave_peak` | Wave peak is safe to retry directly according to current skill metadata. |
| `wave-return` | `wave_arm` | `return_home` | `return_home` | Returning home is also resumable from its own step. |

| variant | style | accuracy | avg us | avg remaining steps | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `restart_skill` | always restart | 20.0% | 0.18 | 3.60 | 100 | 100 |
| `resume_failed_step` | direct retry | 80.0% | 0.16 | 2.00 | 100 | 100 |
| `declared_checkpoint` | metadata-driven | 100.0% | 0.25 | 2.20 | 98 | 100 |
| `conservative_rewind` | safety-first rewind | 20.0% | 0.31 | 3.60 | 71 | 100 |


## Gateway Replanning

Gateway replanning should be explored as loop policies, not hidden as one control-flow implementation.

| case | skill | expected decision | expected resume | why |
| --- | --- | --- | --- | --- |
| `task-complete` | `pick_and_place` | `finish` | `none` | Normal successful execution should stop the loop. |
| `detect-failure-recovery` | `pick_and_place` | `replan_with_recovery` | `detect_object` | Observation failure should schedule a recovery skill while preserving checkpoint context. |
| `grasp-failure-recovery` | `pick_and_place` | `replan_with_recovery` | `move_to_object` | Motor grasp failure should trigger recovery skill selection and keep the pre-grasp checkpoint. |
| `recovery-complete` | `recover_grasp` | `resume_original` | `move_to_object` | Completed recovery work should return to the original instruction. |
| `budget-exhausted` | `pick_and_place` | `stop_failed` | `none` | When the budget is exhausted the loop should stop instead of spinning. |
| `wave-no-recovery-checkpoint` | `wave_arm` | `direct_resume` | `move_to_wave_peak` | Without a dedicated recovery skill, the loop should prefer direct checkpoint resume over generic replanning. |

| variant | style | accuracy | avg us | avg followup rounds | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `current_gateway_clone` | current baseline | 83.3% | 0.14 | 1.17 | 80 | 100 |
| `fail_fast` | stop on error | 33.3% | 0.12 | 0.00 | 100 | 100 |
| `direct_resume_first` | checkpoint first | 66.7% | 0.15 | 0.67 | 80 | 100 |
| `resume_aware_replan` | hybrid loop | 100.0% | 0.17 | 1.00 | 71 | 100 |


## Backend Observation Fusion

Backend observation fusion should evolve through competing summaries of sensor and backend state before replanning.

| case | failed tool | expected hint | why |
| --- | --- | --- | --- |
| `detect-miss-home` | `sensor` | `reobserve_target` | A sensor miss should preserve backend pose but ask for re-observation. |
| `grasp-failure-target-visible` | `motor_control` | `recover_grasp` | Grasp failure with visible target should fuse robot pose and object pose together. |
| `grasp-conflict-occluded` | `motor_control` | `verify_grasp_state` | If the backend thinks it holds the cube but the sensor misses it, fusion should preserve the grasp state and request verification. |
| `place-failure-still-holding` | `simulator` | `retry_place` | If place failed and the robot still holds the object, the fused context should preserve bin pose and retry placement. |
| `wave-motion-no-sensor` | `simulator` | `resume_motion` | Motion-only skills should still carry backend pose forward without fabricating target observations. |
| `placement-verified-by-sensor` | `sensor` | `placement_verified` | A confident sensor hit at the bin should confirm placement rather than trigger unnecessary recovery. |

| variant | style | accuracy | avg us | avg signals | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `backend_authoritative` | backend first | 33.3% | 0.18 | 2.67 | 92 | 100 |
| `sensor_first` | sensor first | 33.3% | 0.17 | 2.17 | 79 | 100 |
| `confidence_weighted` | confidence weighted | 50.0% | 0.18 | 2.67 | 69 | 96 |
| `failure_aware_merge` | failure aware merge | 100.0% | 0.21 | 2.67 | 63 | 70 |


## Tool Output Validation

Tool output validation should evolve through comparable matching contracts instead of freezing one expectation matcher into the runtime.

| case | expected match | why |
| --- | --- | --- |
| `exact_subset` | `true` | Baseline subset case should pass under every strategy. |
| `normalized_pose` | `true` | Pose labels often differ only by case and punctuation. |
| `numeric_string` | `true` | Simulators and REST bridges often stringify numeric confidence values. |
| `bool_string` | `true` | Legacy tools may serialize booleans as strings. |
| `array_superset` | `true` | Outputs may add extra waypoints while still containing the expected sequence members. |
| `array_unordered` | `true` | Evidence arrays are often unordered. |
| `missing_field` | `false` | Missing required fields must fail under all variants. |
| `wrong_target` | `false` | Semantically different targets must not match. |

| variant | style | accuracy | avg us | avg checked fields | readability | extensibility |
| --- | --- | --- | --- | --- | --- | --- |
| `strict_subset_clone` | exact subset | 37.5% | 0.33 | 1.38 | 100 | 100 |
| `normalized_scalars` | scalar normalization | 75.0% | 1.08 | 1.38 | 100 | 100 |
| `array_membership` | array membership | 62.5% | 0.35 | 1.38 | 100 | 100 |
| `tolerant_contract` | tolerant contract | 100.0% | 2.57 | 1.38 | 100 | 100 |

