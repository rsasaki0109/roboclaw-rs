# Decisions

Generated by `cargo run --example planner_experiments -- --write-docs`.

## Accepted Now

- Keep stable runtime abstractions narrow and product-facing.
- Put alternative designs into experiment suites with shared cases and generated comparison docs.
- Use provisional references only to guide the next experiment cycle; do not treat them as final architecture.

## Narrow Stable Core Now

- Keep `roboclaw-agent::Planner -> PlanDecision` as the only stable planning contract. Skill-ranking heuristics and provider adapters remain experimental, but catalog-constrained planning is now the narrow boundary that survives the current suite set.
- Keep `roboclaw-skills::SkillCatalog` plus YAML execution metadata stable. The current runtime can stay narrow around `expect`, `max_retries`, `resume_from_step`, `recovery_for`, `resume_original_instruction`, and `supports_checkpoint_resume`.
- Keep the gateway loop stable only at the orchestration level: `instruction -> plan -> execute -> observe -> memory -> resume/replan`. The winning recovery surfaces reinforce metadata-driven control rather than new type layers.
- Keep `roboclaw-tools::Tool`, `roboclaw-sim::RobotBackend`, `roboclaw-ros2::Ros2Bridge`, and `roboclaw-memory::Memory` as product-facing boundaries. Exact reduction, fusion, and matcher policies stay replaceable.
- Keep provenance, promotion, rollback, mirroring, and lag-calibration suites outside the runtime core. They are converging on budget-style governance, but that evidence should constrain policy, not widen the robot runtime API.

## Cross-Provider Validation

- Available providers in this environment: `mock`.
- Frontier set among available providers: `mock`.
- Provisional reference: `mock` because it stayed on the frontier with the lowest average latency (0.03 ms).
- Keep live provider validation outside the stable runtime because availability, API keys, and local model state are environment-specific.

## Cross-Suite Contradiction Detection

- Frontier set: `evidence_graph`.
- Provisional reference: `evidence_graph` because it stayed on the frontier while using the richest contradiction evidence (6.00 signals).
- Keep contradiction detection experimental until more promotion and rollback cycles create real cross-suite mismatches.

## Cross-Train Lag Carryover

- Frontier set: `carryover_budget`.
- Provisional reference: `carryover_budget` because it stayed on the frontier while using the richest carryover-evidence window (6.00 signals).
- Keep cross-train carryover policy experimental until the repo has real multi-cut publication traces with unresolved lag debt.

## Frontier Snapshot Replay

- Frontier set: `weighted_stability`.
- Provisional reference: `weighted_stability` because it stayed on the frontier while using the broadest evidence window (3.33 snapshots).
- Keep promotion rules experimental until live provider validation produces real snapshot histories for more than one environment.

## Planner Selection

- Frontier set: `catalog_score`, `pipeline_router`.
- Provisional reference: `catalog_score` because it stayed on the frontier and had the best balance between readability (59) and extensibility (100).
- Keep all planner variants experimental until additional cases force convergence on a smaller minimum interface.

## Artifact Trust Decay

- Frontier set: `trust_decay_budget`.
- Provisional reference: `trust_decay_budget` because it stayed on the frontier while using the richest cross-train evidence window (6.00 signals).
- Keep artifact trust policy experimental until the repo has real release-train disagreements and rollback-note history.

## Artifact Mirroring Drift

- Frontier set: `mirror_budget`.
- Provisional reference: `mirror_budget` because it stayed on the frontier while using the richest mirror-evidence window (6.00 signals).
- Keep mirror-drift policy experimental until the repo has real publication lags across registries and documentation portals.

## Provenance Lag Budgets

- Frontier set: `lag_budget`.
- Provisional reference: `lag_budget` because it stayed on the frontier while using the richest lag-evidence window (6.00 signals).
- Keep publication-lag policy experimental until the repo has real staggered publication traces from registries, release feeds, and docs portals.

## Cross-Repo Provenance Stitching

- Frontier set: `stitching_budget`.
- Provisional reference: `stitching_budget` because it stayed on the frontier while using the richest cross-repo evidence window (6.00 signals).
- Keep cross-repo stitching experimental until the repo has real release artifacts split across multiple repositories.

## Provenance Backfill

- Frontier set: `artifact_lineage_budget`.
- Provisional reference: `artifact_lineage_budget` because it stayed on the frontier while using the richest artifact-evidence window (6.00 signals).
- Keep artifact backfill experimental until the repo has real changelog, release-note, and rollback-note history to mine.

## Promotion Environment Provenance

- Frontier set: `environment_lineage_budget`.
- Provisional reference: `environment_lineage_budget` because it stayed on the frontier while using the richest environment-evidence window (6.00 signals).
- Keep deployment-environment provenance experimental until the repo has real staged rollout records beyond synthetic cases.

## Promotion Provenance

- Frontier set: `documented_chain`, `lineage_budget`.
- Provisional reference: `lineage_budget` because it stayed on the frontier while using the richest release-history evidence (6.00 signals).
- Keep provenance policy experimental until multiple real release cuts and changelog practices exist in the repo.

## Promotion Rules

- Frontier set: `stability_budget`.
- Provisional reference: `stability_budget` because it stayed on the frontier while considering the richest promotion evidence (6.00 signals).
- Keep promotion policy experimental until more suites accumulate real replay histories and stable/runtime boundaries change under pressure.

## Planner Prompt Shaping

- Frontier set: `prompt_constrained`.
- Provisional reference: `prompt_constrained` because it stayed on the frontier while keeping prompt size bounded (1185.83 chars).
- Keep prompt shaping outside the stable planner path until provider comparisons confirm which constraints need to live in prompt text versus schema.

## Recovery Skill Patterns

- Frontier set: `checkpoint_aware_specialist`.
- Provisional reference: `checkpoint_aware_specialist` because it stayed on the frontier while keeping recovery sequences short (1.67 average steps).
- Keep recovery patterns experimental until real failure traces force convergence on which recoveries deserve first-class YAML skills.

## Rollback Rules

- Frontier set: `rollback_budget`.
- Provisional reference: `rollback_budget` because it stayed on the frontier while using the richest rollback evidence (6.00 signals).
- Keep rollback policy experimental until promoted surfaces actually survive and fail across multiple runtime cycles.

## ROS2 Observation Ingestion

- Frontier set: `stream_reducer`.
- Provisional reference: `stream_reducer` because it stayed on the frontier while retaining the richest context (4.00 fields).
- Keep ROS2 ingestion policy outside the stable runtime until real delayed and out-of-order telemetry traces expand the case set.

## Surface Lag Budget Calibration

- Frontier set: `trace_budget`.
- Provisional reference: `trace_budget` because it stayed on the frontier while using the richest trace-evidence window (6.00 signals).
- Keep surface-specific lag calibration experimental until the repo has trace exports from real package, feed, and docs publication pipelines.

## Resume Policy

- Frontier set: `declared_checkpoint`.
- Provisional reference: `declared_checkpoint` because it stayed on the frontier while minimizing remaining steps (2.20) without sacrificing accuracy.
- Do not move resume policy into a larger abstraction yet. Keep it as an experiment surface until more recovery cases exist.

## Gateway Replanning

- Frontier set: `resume_aware_replan`.
- Provisional reference: `resume_aware_replan` because it stayed on the frontier while minimizing followup rounds (1.00).
- Keep loop policy experimental until cases include richer backend-state transitions and repeated recovery chains.

## Backend Observation Fusion

- Frontier set: `failure_aware_merge`.
- Provisional reference: `failure_aware_merge` because it stayed on the frontier while keeping richer fused context (2.67 signals).
- Keep fusion policy outside core until ROS2/Gazebo observations add richer conflict cases.

## Tool Output Validation

- Frontier set: `tolerant_contract`.
- Provisional reference: `tolerant_contract` because it stayed on the frontier while keeping the broadest reusable matcher surface (extensibility 100).
- Keep the runtime matcher narrow until real tool outputs add more nested arrays, numeric coercion, and ambiguity cases.

## Next Suites

- Topic QoS and delayed-telemetry tolerance across ROS2 transports.
- Drift-aware case generation when a promoted reference starts regressing across snapshots.
- Promotion handoff rules for moving a provisional frontier into the stable core without copying its full experimental machinery.
